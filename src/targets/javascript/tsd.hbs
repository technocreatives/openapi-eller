{{#each groups}}
{{#if name}}
    // Section: {{name}}
{{/if}}
{{#each endpoints}}

    @{{{httpMethod}}}("{{{url}}}")
    fun {{{operationId}}}{{{operationParams}}}: Promise<{{{returnType}}}>
{{/each}}
{{/each}}